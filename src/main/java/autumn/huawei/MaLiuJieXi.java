package autumn.huawei;

import java.util.Scanner;

public class MaLiuJieXi {

    /*
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String line = sc.nextLine();
        String[] arguments = line.trim().split(" ");
        int length = Integer.parseInt(arguments[0]);
        // 先找同步头
        int start = 1;
        for(; start < arguments.length; start++) {
            if(arguments[start].equals("0x00") || arguments[start].equals("0x0")) {
                break;
            }
        }
        if(start >= arguments.length) {
            System.out.println("FALSE");
            return;
        }
        // 然后开始找序号标识
        start++;

    }
    */
}
